# Plan 19 — Unity-like Editor 独立应用（PixelEngine.Editor.Shell）

## 编辑器产品化修复（2026-07-11）

真实实例工程走查确认，M13 的自动化结构闭合并不等于产品交互已经正确。当前实现把 Editor 当前场景与 Project StartScene 混为一体，导致浏览测试场景会改写受版本控制的启动配置；所有主要转场缺少统一 dirty guard；Recent 早于 Session 成功写入且 scripted probe 污染真实用户目录；Project Window 只扫描 Content，却把 Script 创建到不参与项目 ScriptSource 编译的路径；Asset 查询还会在 Inspector 帧循环中递归扫盘、解析全库并重写 manifest。上述均为确定性实现缺陷，不得继续归类为“只缺人工 reviewer”。

产品化修复按四层收敛。第一层是用户级 workspace：显式 CLI 优先，其次恢复最后一次成功工程与每工程 last scene，Project StartScene 只由 Project Settings 修改；New/Open Scene、切换/关闭工程和 Exit 共用 Save/Don't Save/Cancel 转场协调器，失败保持原 Session/Undo/选择，关键文档走原子替换，scripted probe 使用隔离 user-data。第二层是 Asset Database：Content 与 ScriptSource 是显式 logical root，查询纯内存、增量失效，manifest 写入只发生在索引变更提交时，stable id 贯穿选择与操作。第三层是 Project Window：采用 folder tree、breadcrumb、直接子项与上下文操作，资源 descriptor 提供本地化类型、用途、badge 和摘要，Scene/Script 双击走各自主操作。第四层是 Scene authoring：Scene View 拥有独立 camera/authoring 语义，消费声明式初始 world 或受控 procedural preview，并绘制网格、边界与对象 overlay；Editor 和 Player 必须使用同一项目脚本与世界来源，不得再用同全名空壳 Behaviour 冒充 dogfood。

对应 live 状态只在 `plan/tasks/50-product-editor-ui-demo.md` 的 `EDITOR-004`–`EDITOR-007` 维护；本节仅作为详细设计与边界依据，不新增历史 checkbox。

`EDITOR-004` 已实现用户级 `editor-workspace.json`、自动恢复优先级、异常退出抑制、窗口尺寸恢复、隔离 user-data、Project Picker 在有/无工程间可逆切换、统一 dirty 转场协调器，以及 Scene/Project/Recent 的原子写入。当前编辑场景与 Project StartScene 已彻底分层；缺失或损坏场景会保留原状态并给出诊断。全量 Hosting 中发行/性能证据工具测试会执行大量外部脚本，不属于本节点回归入口；本节点使用非发行工具集与 Editor 全量测试闭合，证据数字记录在 canonical task。

`EDITOR-005` 已把 Asset Database 收敛为 `Content` / `ScriptSource` 双 logical root 与各自独立 manifest；Project Window 查询只读取内存快照，显式刷新和双根 watcher 才更新索引。外部创建、修改、删除和重命名按有界批次消费，watcher 溢出退化为单根完整重扫；唯一文件身份可延续 stable id 并同步 typed reference、UI manifest 与 Scene settings，身份歧义则拒绝猜测并显示诊断。manifest 采用条件原子写入和损坏隔离恢复，Script 创建与双击打开均解析到项目真实 `ScriptSourcePath`；资产/文件夹选择以 stable id 和路径迁移共同恢复。

`EDITOR-006` 已把 Project Window 重构为左侧双根 folder tree 与右侧 breadcrumb / 直接子项导航；深层资产只在搜索态展开并显示完整路径。New、Import、Refresh 和选中资产主操作位于工具栏，移动、重命名与删除收进 stable-id 绑定的工具栏/上下文菜单，Script 与非 Script 创建会自动路由到兼容 logical root。资产 descriptor 在索引刷新时生成本地化类型、工程用途、静态测试 badge 和 schema 摘要；StartScene / CurrentScene badge 仅比较内存工程与 Session 状态。Scene 双击走 `EditorShellApp.OpenSceneAsset` 与统一 dirty guard，不修改 Project StartScene；搜索覆盖 path、enum/localized type、用途、摘要、badge 与 stable id。Demo 的材质、反应、启动、武器、音频 Cue/clip、UI manifest/screen、字体、材质地图、材质纹理、probe 场景与真实 ScriptSource 均有直接可读语义。

## Unity 6.5 视觉与交互对标增补（2026-07-12）

`EDITOR-003` 的真实窗口参照固定为 Windows Unity 6.5 `6000.5.3f1`，基线工程使用 `D:\Project\BallWorld` 的默认布局。对标范围是 PixelEngine 已公开并可完成真实工作的 Editor 表面；不要求复制 Unity 未进入 PixelEngine 产品范围的包管理、云服务或 3D 专用系统，也绝不允许为视觉相似加入无行为 stub。一个能力一旦出现在 PixelEngine 菜单、工具栏或面板中，就必须拥有可理解、可撤销/恢复的真实行为，并在视觉层级和交互反馈上符合 Unity 用户预期。

首轮同机对照把差异分成六个持续验收面：

| 验收面 | Unity 6.5 参照 | PixelEngine 收敛要求 |
| --- | --- | --- |
| 全局 chrome | 扁平紧凑的菜单/工具栏、中央 Play 组、右侧 Layout、底部状态与任务反馈 | 去除宽大的常驻文件操作，统一中性深灰密度；Play 组严格居中且活动态为 Unity 蓝；真实状态移到底栏，Layout 入口只暴露已接线行为 |
| 核心工作台 | Scene 为主视图，Hierarchy/Project/Inspector/Console 以紧凑 tab/dock 组织 | 默认布局、tab、分隔线、关闭按钮、最小尺寸和 720p/高 DPI 行为与参照同构，面板不得互相覆盖或截断 |
| 选择与 Inspector | Hierarchy 蓝色选择持续驱动 Inspector；GameObject header、Transform、组件 foldout 与 Add Component 层级清晰 | 选择颜色、键盘焦点、字段密度、组件折叠/启用/增删/重排及 Undo/Redo 反馈一致 |
| Scene / Game | Scene 工具、grid/snap、gizmo 与 framing 可发现；Play 后按钮着色并切换 Game View 工具语境 | 所有可见工具均接真实行为；快捷键、hover/tooltip、active/disabled、Play→Pause→Step→Edit 状态无歧义 |
| Project / Console / Picker | 资源树、搜索过滤、行选择、日志等级与首次工程入口使用一致的紧凑层级 | 目录/资产/日志操作、删除确认、drag/drop、错误恢复和 Project Picker 信息架构需用真实鼠标键盘闭环 |
| 窗口系统 | resize、DPI、IME、焦点、停靠与 Windows Graphics Capture 可稳定观察 | framebuffer、屏幕录制和输入坐标必须一致；白屏捕获、旧 DPI 坐标、透明区域误捕获均视为未完成 |

Hierarchy / Inspector 的 Unity 对标必须服从 PixelEngine 2D authoring 真相源：Hierarchy 提供真实场景搜索、对象图标、Scene Visibility、Scene Picking、选择与层级操作，其中 visibility/picking 只影响编辑态 Scene View，不得偷换为 GameObject active；Inspector 以 active + name header、始终存在的 Transform component、Behaviour component foldout/context menu 和 Add Component 搜索组织现有能力。PixelEngine 尚无 Tag、Layer、Static 或 3D Z 轴产品语义，因此不得为视觉相似绘制这些无行为字段。GameObject `Enabled` 一旦暴露为 active 开关，就必须随 `.scene` / prefab 往返，并在运行时物化时递归考虑父级禁用状态；不能继续停留为保存后丢失的编辑器临时位。Scene Visibility / Picking 则是非落盘的编辑器状态：父级隐藏/禁止拾取递归作用于子级，分别真实控制 marker 绘制与鼠标命中/gizmo，不得用无行为图标占位。

Project / Console 的默认 chrome 同样按 Unity 6.5 的高频信息层级收敛：Project 常态只保留 `+` 创建入口、搜索/类型过滤、树/内容双栏、breadcrumb、grid/list 与底部缩放/计数，refresh/sort/import 等低频操作进入 options 或 context menu，不能把 rename/delete 等 selection action 长期铺满顶栏；列表行保持单行紧凑，详情交给 Inspector。Console 使用单行 Clear/Collapse/Clear on Play/Error Pause、右侧搜索和带计数的 Log/Warning/Error toggle，主体为可选日志列表并在下方保留稳定详情区；不得因窗口变窄把一组功能固定拆成三层工具栏。两者所有迁移后的动作仍须真实可达，不能用“视觉精简”删除创建、导入、拖放、错误恢复或源码定位。

交付按“全局主题/chrome/status → 核心面板密度与选择 → Scene/Inspector 操作 → Project/Console/Picker → resize/DPI/录屏 → 完整 author→play→edit→build→run”循环推进。每轮必须保留 Unity 参照、PixelEngine 当前帧、输入动作与自动化结果；脚本化 source-contract 只能作回归门禁，不能替代真实窗口 reviewer。只有差异矩阵清零、同机复走完整路线且没有可复现的视觉或交互偏差时，canonical `EDITOR-003` 才能改为完成。

> **状态迁移（2026-07-10）**：本文件保留详细设计与历史 checkbox；当前状态、顺序和完成条件以 [`plan/tasks/README.md`](tasks/README.md) 为唯一真相源。不要在本文件新增 live task；设计变化仍须同步到这里。

> **DOC-002 历史证据口径（2026-07-10）**：后文 checkbox 与“已通过/已完成”叙述冻结自旧计划快照 `179efc3a`，迁移基线为 `5af1541f`，均不构成 live 状态；证据等级以 [稳定 Evidence Index](../docs/evidence-index.md) 为准。未入索引的 `artifacts/`、`BenchmarkDotNet.Artifacts/`、`scratch/` 仅是可再生历史线索；替代报告与重跑命令见 [DOC-002 校正报告](../docs/evidence-2026-07-10-doc-002-legacy-plan-audit.md)。

> **DOC-002 Editor 证据校正**：后文 DPI 截图、默认工作台玩家包与 `build-result.json` 的 `artifacts/` 路径只记录历史本地 probe；可依赖边界是 canonical `BASE-013`，重跑命令见 DOC-002 报告，真实窗口首次启动 / Build And Run / 完整 UX 仍由 `EDITOR-*` 管理。

> 产品依据：`../docs/PixelEngine-核心目标与产品定位.md`。本文件是 Unity-like Editor 状态账本，负责独立编辑器壳、项目/资源/Hierarchy/Inspector/Scene View/Game View/Console/Settings/Prefab/Build Settings、EditorShell 与玩家包解耦，以及 M13 结构闭合、M14 UX Contract、M15 人工 UX 证据。
> 状态约定：`- [x]` 已有源码、测试、工具、报告或 plan 证据；`- [ ]` 未完成目标；`- [!]` 外部证据债、人工验收、硬件/native/发行/真实窗口阻塞。本文不再使用进行中状态，所有部分完成事项拆成已完成子项与未完成/阻塞子项。

### 2026-07-10 Editor Preferences 设计补充

EditorShell 必须区分工程级 Settings 与用户级 Preferences：`Project Settings` / `Player Settings` / `Build Settings` 继续随工程入盘；`Edit > Preferences...` 独立保存在用户 AppData，且在 ProjectPicker 与工程工作台两种状态都可打开。Preferences 使用左侧类别导航，首批类别为 Appearance、General、External Tools 与 Shortcuts；只展示已经接入真实行为的选项，不提供无效的语言或快捷键编辑占位。

`UI Scale` 是 Editor 视觉密度的单一权威值，范围 75%–200%，标准档包含 100%、125%、150%、175%、200%。它必须同时驱动启动时 CJK 字体 atlas 尺寸、当前 ImGui context 的字体显示比例、主题 padding/spacing 等 style metrics，以及 Shell 顶部工具栏等显式像素尺寸；不能只放大文字。偏好变更立即应用并原子持久化，重启后以目标字号重新构建字体 atlas，避免 4K 屏长期使用位图放大。`Save layout on exit` 与 `External script editor` 属于用户级设置；旧 `ProjectSettingsDto.EditorPreferences` 暂留为只读兼容来源并在首次加载全局偏好时迁移，不再出现在 Project Settings 面板，也不再由工程设置探针写入。

实现状态：commit `8d8598fc` 已完成上述自动化切片，并把默认可见面板收敛为 Hierarchy、Scene / Game View、Inspector、Project / Console；其余窗口经分类 Window 菜单按需打开。稳定自动化报告见 [`docs/evidence-2026-07-10-editor-preferences-ui-scale.md`](../docs/evidence-2026-07-10-editor-preferences-ui-scale.md)。真实 4K 显示器人工操作、菜单点击与完整 author→play→edit→build→run reviewer 仍归 M15，不因 scripted framebuffer probe 关闭。

---

## 1. 当前产品职责

- [x] **Unity-like Editor 产品面**：交付一个独立 EXE、独立顶层窗口、单进程 in-process 宿主 Engine 的编辑器应用，面向 Unity 用户提供 Project、Hierarchy、Inspector、Scene View、Game View、Console、Settings、Prefab 与 Build Settings 心智模型。
- [x] **Editor ImGui 面板层宿主**：编辑器产品面复用 `PixelEngine.Editor` 的 Editor ImGui 面板层，但面板层本体仍归 plan/12；本文件负责 Shell 顶层产品流、窗口生命周期、项目/session、authoring 与面板编排。
- [x] **Engine runtime 消费者**：EditorShell 只消费 Hosting 公开 API：窗口/GL attach、Edit/Play/Step、`.scene` writer、运行时物化、持久世界存读档、build-player 子进程入口。
- [x] **玩家包解耦边界**：玩家运行时与编辑器工具包分离；player 包不含 `PixelEngine.Editor.dll`、ImGuizmo/ImPlot 等编辑器专属闭包，允许玩家 HUD 所需 `Hexa.NET.ImGui` 核心。
- [x] **非职责边界**：本文件不实现 CA/Physics/Rendering/Audio、脚本编译、Editor ImGui 面板内部逻辑、Web-first UI 后端本体、Demo 玩法内容或 plan/15 打包脚本内部逻辑。

## 2. 状态总览 checklist

- [x] **M13 结构闭合**：`PixelEngine.Gui` 中性化、Hosting 去 Editor 硬引用、`apps/PixelEngine.Editor.Shell`、单窗口/单 GL、ProjectPicker、in-process Engine attach、Edit/Play/Step、GameObject authoring、`.scene` v2、Prefab、Build Settings 面板、build-player 子进程编排与 player-only audit 均已落地。
- [x] **M13 编排证据闭合**：shell scripted probe 已覆盖打开工程、Edit 装配、Play/Exit 回滚、保存场景、关闭工程、同窗口重开工程、编辑器内 Build/Build And Run、失败诊断、取消后重跑和设置持久化。
- [x] **M14 Unity-like UX Contract Settings 同源切片已产品化（自动化部分）**：Project / Player / Build Settings 均绑定 plan/18 的 Hosting DTO/store；Project / Player Settings 支持读写、校验错误提示与 scripted probe，Project Settings 已驱动 EditorProject/EngineProject 入口，Player Settings 已同源投影到 headless/runtime options、build-player 参数、`build-result.json` 与 packaged `startup.json`。ProjectPicker 已接入 Windows 原生文件夹选择器，Browse 会把当前输入目录或 `.pixelproj` 文件路径作为默认目录；ProjectPicker 启动阶段与进入工程后的 Editor 输入桥均会为 Ctrl+V/C/A 同步 ImGui `ModCtrl` 聚合修饰键，保证文本框剪贴板快捷键进入 ImGui 编辑路径；Project Window 资产语义、搜索 / 类型过滤 / 排序、只读预览摘要、删除确认/引用安全预检、资产拖拽语义、脚本外部编辑器、Console 与 Game View 自动化契约底座已闭合；真实窗口人工 UX 证据仍保留为 M15 `[!]`。
- [x] **Unity 6 视觉基础切片（自动化部分）**：中性 `PixelEngine.Gui` 新增可测试 `GuiThemeKind` / `GuiThemeTokens`，EditorApp 默认使用 `Unity6Dark`，EditorShell 启动阶段 ProjectPicker 也显式使用同一 Unity 6 深灰主题；玩家侧中性 GUI 默认仍为 `NeutralDark`，避免把玩家 fallback HUD 强行套用编辑器皮肤。核心窗口标题已对齐 Unity 心智模型：`Scene`、`Game View`、`Hierarchy`、`Inspector`、`Project`、`Console`、`Profiler`、`Project Settings`、`Player Settings`、`Build Settings`，默认 dock layout 继续通过同源常量停靠这些窗口。证据：`EditorAppTests.EditorDefaultsToUnity6DarkThemeWithoutChangingNeutralGuiDefault`、`EditorAppTests.DockSpaceDefaultLayoutDeclaresViewportAndToolWindows`、`GuiAppCombinedFrameTests`。真实窗口观感、密度、hover/drop 手感与人工 UX 证据仍归 M15 `[!]`。
- [x] **Unity 6 顶部工具栏切片（自动化部分）**：`EditorMainMenuBar` 在菜单栏下方新增 viewport sidebar 顶部工具栏，左侧暴露 New Project / Open Project / Save Scene / Build，中央暴露 Play / Pause / Step，右侧状态文本显示当前 Project、Scene dirty 标记、Edit/Play 模式与 GameObject 数量；状态由 `EditorMainToolbarState` 从真实 `EditorProjectSession` / `EditorPlaySessionSnapshot` 捕获，不引入第二套编辑器状态。`EditorApp` 新增 `IEditorChromePanel` 调度层，主菜单/工具栏在 dockspace 前绘制，确保顶部栏参与 ImGui work area 预留而不是覆盖 Scene/Game/Hierarchy。证据：`EditorAppTests.DrawFrameDispatchesDockSpacePanelsAndRenderInOrder` 锁定 Chrome → DockSpace → Panel 顺序，`HostingProjectDisciplineTests.EditorShellMainMenuDeclaresRequiredMenus` 锁定工具栏入口、`BeginViewportSideBar`、项目/保存/构建/Play 控制和 dirty/play 状态来源。真实窗口点击命中、hover 反馈与人工 UX 证据仍归 M15 `[!]`。
- [x] **M14 Settings 同源 schema 自动化闭合**：Project Settings / Player Settings / Build Settings 已绑定 plan/18 的 `ProjectSettingsDto` / `PlayerSettingsDto` / `BuildProfileDto`，Shell 只保留面板与 adapter，不形成第二套 settings schema；真实窗口填写、重启恢复和人工 UX 证据仍归 M15 `[!]`。
- [!] **M15 人工 UX 证据未闭合**：真实窗口完整路线视频、人工 UX 走查、脚本外部编辑器、资产引用稳定性、Project Window 拖拽/移动/重命名不破坏引用等需要人工/真实窗口证据；scripted probe 不能替代最终 UX 复核。

## 3. M13 结构完成 checklist

- [x] 新增 `src/PixelEngine.Gui` 中性程序集，承载 `HexaImGuiBackend`、中性 `IGuiContext` 运行时适配、`GuiFontManager` 与 GUI render bridge；`PixelEngine.Editor` 只保留编辑器专属面板、dockspace、ImGuizmo/ImPlot 绑定。
- [x] `PixelEngine.Hosting` 删除对 `PixelEngine.Editor` 的编译期引用，改为通过 `IEditorHostExtension` / 相位 [10] 钩子由 EditorShell 注入 Editor 面板宿主。
- [x] `demo/PixelEngine.Demo` 去除 `using PixelEngine.Editor` 与 `EnableEditor` 路径，玩家 HUD 通过 `PixelEngine.Gui` 中性 host 工作，玩家闭包不再传递到 Editor。
- [x] 新增 `apps/PixelEngine.Editor.Shell` 可执行项目，引用 `{Hosting, Editor, Gui}`，位于 Demo 同层；Shell 启动即创建唯一 `RenderWindow`、唯一 GL context 和中性 ImGui host。
- [x] ProjectPicker / `EditorProject` / `RecentProjectsStore` 已支持新建工程、打开工程、最近工程、`project.pixelproj` 读写、路径逃逸拒绝与 content/scenes 骨架生成。
- [x] `EditorProjectSession` 已通过 Hosting attach 外部窗口，Engine 不 own window，关闭/切换工程只释放 Engine 并保留 Shell 窗口/GL context。
- [x] `EditorMainMenuBar` 与默认 dock 布局已提供 File/Edit/GameObject/Window/Play/Help，默认显示 Hierarchy、Scene View、Inspector、Project、Console、Performance HUD、Build Settings 等工作区。
- [x] `EditorSceneModel` / `EditorGameObject` / `EditorComponentModel` 已成为 authoring 真相源；运行时保持扁平 DOD，父子层级在物化时烘焙为世界 TRS。
- [x] Hierarchy 已支持创建、删除、重命名、复制、启用位、拖拽重父、防环、选中联动与 Undo/Redo 命令栈。
- [x] Inspector 已支持 Name、Enabled、StableId、Transform TRS、组件列表、Add/Remove Component、字段反射编辑与命令栈回滚。
- [x] Scene View 已复用 viewport，支持 Edit 相机、ImGuizmo 平移/旋转/缩放、屏幕空间拾取、空对象 billboard 命中与 gizmo/画刷输入仲裁。
- [x] `.scene` schema 已升到 `FormatVersion=2`，包含 `ParentId` 与 Transform TRS；v1 兼容读取、writer 稳定排序、读写往返等价已由测试覆盖。
- [x] Prefab 已实现资产、实例、override、Revert、嵌套 prefab、override 传播与运行时展开物化。
- [x] Shell 已复用 plan/12 面板：Project 底座、材质+反应编辑器、世界画刷/检视器、调试叠层、性能 HUD、存读档、子系统调参、sim 控制与 Edit/Play 模式面板。
- [x] Build Settings 面板已落在 Shell 程序集，消费 plan/15 `tools/build-player`，支持设置、预检、NDJSON 进度、日志、取消、Build And Run、dev-audit / release audit 布局分流。
- [x] plan/15 audit 已拒绝 player 包 `app/` 内 `PixelEngine.Editor.dll`、ImGuizmo/ImPlot 编辑器闭包，并允许 `Hexa.NET.ImGui` 核心作为玩家 HUD 依赖。

## 4. M14 Unity-like UX Contract checklist

- [x] **ProjectPicker 最近工程启动韧性（自动化切片）**：`RecentProjectsStore.Load` 已对损坏的 `recent-projects.json`、不可读文件、空/非法 ProjectPath、重复路径与超限列表做容错归一化，加载失败只清空最近工程列表，不阻断 EditorShell 启动；最近工程仍按 `LastOpenedUtc` 倒序、同一路径保留最新记录并裁剪到 20 条。证据：`EditorShellProjectTests.RecentProjectsLoadReturnsEmptyStoreWhenJsonIsCorrupted`、`EditorShellProjectTests.RecentProjectsLoadNormalizesEntriesByPathOrderAndLimit`。
- [x] **ProjectPicker 原生文件夹选择器（自动化切片）**：`ProjectPickerWindow` 的 `Browse...` 按钮已接入可注入 `IProjectFolderPicker`，默认通过 `NativeProjectFolderPicker` 调用 Windows `IFileOpenDialog` folder picker；`NativeFolderPicker` 使用 source-generated `SHCreateItemFromParsingName` 将当前输入的目录或 `.pixelproj` 文件路径转换为 ShellItem，并调用 `SetDefaultFolder` / `SetFolder` 作为默认打开目录。`ApplyFolderPicker` 已把 Browse 成功、失败与取消收敛为可测试契约：成功回填路径并清理诊断，失败保留原路径并显示诊断，取消不覆盖输入且会清理旧失败诊断。证据：`EditorShellProjectTests.ProjectPickerBrowseSuccessUpdatesPathAndClearsDiagnostic`、`EditorShellProjectTests.ProjectPickerBrowseFailureKeepsPathAndStoresDiagnostic`、`EditorShellProjectTests.ProjectPickerBrowseCancelClearsStaleDiagnosticAndKeepsPath` 覆盖 ProjectPicker 行为，`HostingProjectDisciplineTests.EditorShellProjectPickerUsesNativeFolderDialogWithInitialPath` 锁定 Browse 调用、ShellItem 创建、默认目录设置和不再忽略 `initialPath`；真实窗口点击、系统对话框可用性与粘贴/输入法手感仍归 M15 `[!]`。
- [x] **ProjectPicker / Editor 文本输入剪贴板快捷键（自动化切片）**：中性 `GuiInputBridge` 与 Editor `ImGuiInputBridge` 已在左右 Ctrl / Shift / Alt 任一键按下或释放时同步 ImGui `ModCtrl` / `ModShift` / `ModAlt` 聚合修饰键；ProjectPicker 路径输入框、Settings 输入框、Project Window 搜索 / 新路径输入与 Inspector 文本字段均能让 Ctrl+V / Ctrl+C / Ctrl+A 等快捷键进入 ImGui 文本编辑路径。证据：`GuiAppCombinedFrameTests.GuiInputBridgePublishesModifierKeysForClipboardShortcuts` 覆盖 ProjectPicker 启动阶段的中性 Gui 输入桥，`EditorAppTests.EditorInputBridgePublishesModifierKeysForClipboardShortcuts` 覆盖进入工程后的 Editor 输入桥；真实窗口粘贴手感、系统剪贴板内容读取与 IME 组合输入仍归 M15 `[!]`。
- [x] **Project Window 资产模型（自动化切片）**：已在 EditorShell 侧新增工程级 asset manifest / stable asset id / logical path / asset type 模型，`AssetBrowserPanel` 通过 Shell 数据源消费 manifest 记录；面板支持文本搜索、资产类型过滤与路径 / 类型 / 最近修改 / 大小排序，并新增基于按钮 + 新 logical path 输入的移动 / 重命名入口；移动请求必须携带 `Path + AssetId + Kind + NewPath`，Shell 数据源回查 manifest 防止旧列表项或类型错配，再交由 manifest / Scene move service 执行。已覆盖 manifest 生成/重载、常见资产创建、prefab 移动/重命名后 stable id 不变、场景与活动 authoring 模型 prefab 引用重写、Project Window move request 重写 `.scene/.prefab` 内 encoded typed asset reference，以及同路径资产刷新为新 id 后旧移动确认失效；移动资产前会先解析全部候选 `.scene/.prefab` 引用文档，损坏文档会在 `File.Move` 和 manifest 改写前阻断，避免资产已移动但引用未重写的半提交状态；引用文档保存中途失败会回滚已写文档、manifest 与磁盘文件，且活动 authoring 场景仍保留旧 typed asset reference；移动流程会先确认移动后的 manifest asset record，再重写活动 authoring 场景，减少失败路径污染内存态引用的窗口；Scene 资产移动经 Shell 数据源调用 `EditorProjectSceneAssetMoveService` 同步 `project.pixelproj`、Project Settings、Player Settings、Build Settings 与 `content/startup.json`，保持 BuildRequest 的启动场景和入包场景不漂移。引用重写范围明确限定为 `.scene/.prefab` 中的 PrefabLink 与 `assetref|...` typed asset reference，不覆盖任意 JSON、脚本源码或裸字符串路径。证据：`EditorProjectAssetModelTests`、`AssetBrowserPanelTests.AssetBrowserPanelFiltersByKindAndSortsAssets`、`AssetBrowserPanelTests.AssetBrowserPanelMovesOnlyStableAssetsThroughCallbackAndRefreshes`、`AssetBrowserPanelTests.AssetBrowserPanelInvalidatesMoveConfirmationWhenAssetIdChanges`。
- [x] **Project Window 删除确认 + 引用安全预检（自动化切片）**：Project Window 删除请求必须携带 stable asset id / logical path / asset type，Shell 数据源会回查 manifest 防止旧项或类型错配；删除前扫描活动 authoring scene 与 `.scene/.prefab` 文档中的 prefab link / typed asset reference，存在引用时拒绝删除并返回引用位置，无引用资产需二次确认后才删除磁盘文件与 manifest 记录；二次确认状态绑定原始 stable asset id / logical path / asset type，同路径资产刷新为新 id 后旧确认失效并要求重新请求删除。证据：`EditorProjectAssetModelTests.DeleteAssetPreflightBlocksReferencedAssetsAndReportsLocations`、`EditorProjectAssetModelTests.DeleteAssetRequiresConfirmationAndRemovesUnreferencedManifestRecord`、`AssetBrowserPanelTests.AssetBrowserPanelDeletesOnlyStableAssetsAfterConfirmation`、`AssetBrowserPanelTests.AssetBrowserPanelInvalidatesDeleteConfirmationWhenAssetIdChanges`。
- [x] **Project Window 只读预览摘要（自动化切片）**：Project Window 数据源已为纹理、音频、材质 JSON、场景、Prefab、脚本与其它文件生成只读预览摘要，纹理可显示缩略图尺寸，场景 / Prefab 摘要统计 GameObject、根与 Behaviour，脚本摘要提取首个 class 名；面板展示 `预览：...`，并由 `EditorProjectAssetModelTests.ProjectBrowserBuildsPreviewSummariesForCommonAssetTypes` 覆盖。真实窗口预览布局与手感仍归 M15 `[!]`。
- [x] **Project Window 常见资产创建（自动化切片）**：Project Window 面板新增 Create Type / New Asset / Create 入口，通过 `AssetBrowserCreateRequest` 交给 Shell 数据源创建 Folder、Material、Scene、Prefab、Script、UI Screen 与 Json 资产；Folder 创建只落盘真实目录并通过 `IAssetBrowserFolderDataSource.ListFolders()` 回显为空文件夹 drop target，不写入 asset manifest、不参与 stable asset reference；文件资产仍由 Shell 数据源调用 `EditorAssetManifestStore.CreateAsset` 落盘默认 material catalog JSON / `.scene` / `.prefab` / Behaviour 脚本 / `content/ui/screens/*.xhtml` 或 `.html` UI Screen / JSON 模板并刷新 manifest stable asset id，仍拒绝 Texture / Audio 这类需要导入二进制内容的伪创建。Project Window 已追加 Import Type / Source File / Browse Source / Import As / Import 入口，通过 `AssetBrowserImportRequest` 把外部 Texture / Audio 文件复制进 content 并登记 stable asset id，导入目标会跟随选中文件夹并按已有 assets / folders 自动建议不冲突路径；Shell 侧 Browse Source 复用 Windows 原生 `IFileOpenDialog` 文件选择器并保持 Editor 面板层为可注入回调；Shell 数据源只允许 Texture / Audio 导入，拒绝缺失源文件、目标冲突、类型不匹配与 Json 等文本资产伪导入。UI Screen 创建会同步 `content/ui/ui-manifest.json` 的 screen entry，使 manifest 工程可按 screen id 预加载新界面；该自动登记不提供可视化 manifest 编辑、screen 生命周期管理或 plan/20 UI Runtime 完成态。证据：`AssetBrowserPanelTests.AssetBrowserPanelCreatesAssetsThroughCallbackAndRefreshes` 锁定面板回调、刷新与 unsupported 类型诊断；`AssetBrowserPanelTests.AssetBrowserPanelCreatesFolderAndRefreshesEmptyFolderTargets` 锁定空目录回显；`AssetBrowserPanelTests.AssetBrowserPanelImportsTextureIntoSelectedFolderWithUniqueSuggestedPath` 锁定 Texture 导入目标跟随文件夹与唯一命名；`AssetBrowserPanelTests.AssetBrowserPanelBrowseImportSourceUpdatesSourceAndDestination` 与 `AssetBrowserPanelTests.AssetBrowserPanelBrowseImportSourceCancelAndFailureKeepCurrentInput` 锁定 Browse Source 成功 / 取消 / 失败行为；`EditorProjectAssetModelTests.ProjectBrowserCreatesCommonAssetsWithTemplatesAndStableIds` 锁定落盘模板、stable asset id、浏览器数据刷新、Folder 创建与 `ui-manifest.json` 登记；`EditorProjectAssetModelTests.ProjectBrowserImportsTextureAndAudioAssetsWithStableIds` 锁定外部 Texture / Audio 文件复制、stable id、预览摘要与失败诊断；`EditorProjectAssetModelTests.ProjectBrowserCreateFolderRejectsUnsafeAndConflictingPaths` 锁定 Folder 越界/重复/同名文件冲突诊断；`HostingProjectDisciplineTests.EditorShellMainMenuDeclaresRequiredMenus` 锁定 Shell 接线。真实窗口 Create / Import 菜单布局、原生文件选择器弹出、命名输入手感与人工 UX 仍归 M15 `[!]`。
- [x] **Project Window 文件夹拖拽移动语义（自动化切片）**：Project Window 面板已从资产 logical path 推导 content 根目录与子文件夹 drop target；文件夹 drop target 接收现有 stable typed drag payload 后，会回查当前 `AssetId + Path + Kind`，拒绝旧 payload / 类型错配 / 越过 content 根目录的目标，再转换为同一套 `AssetBrowserMoveRequest` 交给 Shell 数据源，因此移动仍复用已有引用预检、`.scene/.prefab` typed reference 重写、Scene settings/startup 同步与目标冲突诊断。证据：`AssetBrowserPanelTests.AssetBrowserPanelMovesDragPayloadToFolderThroughStableRequest`、`AssetBrowserPanelTests.AssetBrowserPanelRejectsStaleDragMovePayloads`。
- [x] **Project Window → Inspector 资产 / Folder 摘要联动（自动化切片）**：`EditorSelection.SelectAsset`、`SelectFolder` 与 `SelectGameObject` 已互斥清理资产 / 文件夹 / GameObject / Entity / Body 选择，避免 Project Window 选中资产或文件夹后 Inspector 继续显示旧 GameObject；EditorShell 现在让 Inspector 与 Project Window 共享同一 `EditorAssetBrowserDataSource`，Inspector 可展示选中资产的 logical path、类型、stable id、大小与只读预览摘要，也可展示选中文件夹的 logical path 与子资产数量，缺失资产或文件夹返回明确诊断。证据：`AssetBrowserPanelTests.EditorSelectionSwitchesBetweenProjectAssetsAndGameObjects`、`AssetBrowserPanelTests.AssetBrowserPanelSelectsFolderAndClearsAssetSelection`、`GameObjectInspectorPanelTests.InspectorPanelCapturesSelectedProjectAssetSummary`、`GameObjectInspectorPanelTests.InspectorPanelCapturesSelectedProjectFolderSummary`、`HostingProjectDisciplineTests.EditorShellDeclaresGameObjectAuthoringModelAndHierarchy`。
- [x] **Project Window → Inspector 资产主操作联动（自动化切片）**：Inspector 选中 Project Window 资产时不再只有只读摘要；Script 资产可从 Inspector 复用 Shell 脚本外部编辑器打开路径，Prefab 资产可从 Inspector 复用 Shell Prefab 实例化路径，结果写回 Inspector 状态并进入 Console 资产分类。证据：`GameObjectInspectorPanelTests.InspectorPanelInvokesProjectAssetPrimaryActions`、`HostingProjectDisciplineTests.EditorShellDeclaresGameObjectAuthoringModelAndHierarchy`。
- [ ] **Project Window 产品剩余项**：真实窗口删除确认 UX、真实窗口拖拽移动 / 重命名引用稳定性证据、脚本外部编辑器人工打开证据、真实窗口预览布局、Settings 等剩余资产创建类型、Project Window → Inspector 资产摘要 / 主操作真实窗口布局与切换手感、常见资产创建 / 导入人工 UX 仍未闭合；当前 create / import / move / rename 自动化入口已包含按钮 + 新路径输入、Browse Source 文件选择器与 folder target drag/drop request，Folder / Material / Scene / Prefab / Script / UI Screen / Json 创建、Texture / Audio 外部文件导入、Folder rename / recursive move / delete、Folder selection、Inspector 资产/Folder 摘要和 Script / Prefab 主操作已有自动化接线；Project Window 选中文件夹会限定资产列表作用域，搜索 / 类型过滤 / 排序在该文件夹作用域内继续工作，选择 content 根目录会恢复全量列表，刷新后当前文件夹消失会回退到根目录；Project Window Create / Import 输入会跟随选中文件夹生成默认 logical path，并按已有 assets / folders 自动建议不冲突的 Unity-like 尾号名称；Folder recursive move 会保留子资产 stable id、批量重写 `.scene/.prefab` typed asset reference / PrefabLink、同步 UI Screen 的 `ui-manifest.json` path，并拒绝会改变资产类型、移动到自身子目录或目标冲突的目录操作；Folder recursive delete 会绑定当前子资产 stable id 集合、预检外部 `.scene/.prefab` 与活动 authoring scene 引用、拒绝仍被引用的子资产、二次确认后递归删除磁盘目录并清理 manifest / UI Screen manifest entry；`UI Manifest` 面板已提供可视化 screen 列表、missing file / missing asset 状态、preload toggle，并可把现有 `content/ui/screens` 下未登记的 UI Screen 同步写入 `ui-manifest.json`。但真实窗口拖拽 hover / drop 手感、资产 Inspector 视觉密度、原生文件选择器实际弹出、UI manifest 可视化编辑手感和人工引用稳定性证据仍归 M15，不得把本自动化切片视为 M15 人工 UX 证据。
- [x] **广义 Inspector 资产字段类型（自动化切片）**：脚本字段可通过 `[AssetField(ScriptAssetKind.*)]` 显式声明 Texture / Audio / Prefab / Scene / Script / Material typed asset reference，Inspector 字段描述携带 `ScriptFieldKind.AssetReference` 与目标资产类型；Project Window 行项已注册 ImGui typed drag payload，GameObject Inspector asset field 已注册真实 drop target，按 `AssetBrowserDragPayload` → `EditorAssetDropPayload.TryFromBrowserPayload` → `EditorAssetDropService.DropOnInspectorField` 写入 stable asset id + logical path + asset type，并将成功/失败诊断同步到 Inspector 状态与 Console；强类型 `ScriptAssetReference` 已通过统一 `SerializedFieldBinder` 接入 EditorShell runtime projection 与 `.scene` loader，覆盖 public property、public field、non-public `[SerializeField]` field，并拒绝 `[HideInInspector]` / readonly 字段；Json/Other 不属于脚本 `ScriptAssetKind` 支持集，现通过 `EditorAssetReferenceCodec.TryEncode` 返回明确失败诊断，直接 `Encode` 也抛出可解释 `InvalidOperationException`，不再留下 `NotImplementedException` 或把任意 JSON/Other 文件误编码为脚本 typed asset reference。证据：`ScriptInspectorTests.InspectFieldsClassifiesTypedAssetReferencesAndSetsCompatibleValues`、`EditorAssetDropServiceTests.InspectorPanelAppliesDescriptorDeclaredTypedAssetFieldDrop`、`EditorAssetDropServiceTests.InspectorPanelAcceptsProjectWindowBrowserPayloadAndRecordsDiagnostics`、`EditorAssetDropServiceTests.JsonAndOtherAssetsAreRejectedFromTypedAssetReferenceEncoding`、`EditorShellSceneMaterializationTests.RuntimeProjectionBakesHierarchyAndBindsStableIdsVector2AndMaterialId`、`EditorShellSceneMaterializationTests.SceneDocumentLoaderBindsScriptAssetReferenceFields`、`EditorShellSceneMaterializationTests.SerializedFieldBindingRejectsHiddenAndReadonlyFields`。
- [x] **资产拖拽语义（自动化切片）**：已定义 Shell 层 typed drag payload / drop result / stable asset reference 编码，验证 prefab 拖拽到 Hierarchy 可按 parent 实例化并 Undo/Redo、prefab 拖拽到 Scene View 可按 world transform 放置、scene/material/script/texture/audio/json/other 拖拽到 Hierarchy / Scene View 均失败并返回诊断、Inspector typed asset 字段可承载 prefab/scene/material/script/texture/audio stable reference 且拒绝 Json/Other 编码、script 拖拽到组件列表可解析并添加 Behaviour，资产移动/重命名会同步重写活动 authoring 模型与 `.scene/.prefab` 文档中的 Inspector asset reference。证据：`AssetBrowserPanelTests.AssetBrowserPanelCreatesTypedDragPayloadOnlyForStableAssets`、`EditorAssetDropServiceTests`。真实窗口 ImGui drag/drop 手感、hover 反馈与人工 UX 证据仍归 M15 `[!]`。
- [x] **脚本外部编辑器（自动化切片）**：脚本资产双击经 Project Window 发起 Shell 层 `EditorScriptAssetOpenService`，configured `ExternalScriptEditor` 优先，空值 / `system-default` 回退 OS/default opener；opener 通过可注入 `IExternalScriptEditorProcessLauncher` 测试 seam 返回结构化结果与诊断，覆盖 `{file}` 占位符替换、无 `{file}` 时自动追加脚本路径、fallback、启动失败、非 script 拒绝、缺文件 / 未登记、路径逃逸。诊断写入 `AssetBrowserPanel.Status` 与 Console 可读状态；真实 OS opener / configured editor 人工打开证据仍归 M15 `[!]`。

`EDITOR-010` 将上述旧“空值=系统默认”契约版本化为 Editor Preferences v2：v1 空值迁移到 VS Code，新版 System Default 使用明确 sentinel，自定义命令继续保留 `{file}`/自动追加兼容并新增 `{line}`/`{column}`/`{project}`。Project Window 双击与 Console source location 共用工程上下文 opener；Unity 对应的 `Assets > Open C# Project` 复用同一 project/solution resolver，VS Code 始终打开 folder/workspace，VS/Rider 才打开包含当前项目的 solution。standalone 生成文件只有带 ownership marker 才可刷新，任何用户 `.csproj/.sln/.slnx` 都只读。
- [x] **Project Settings**：已通过 plan/18 `ProjectSettingsDto` 绑定工程名、content root、script source dir、默认 scene、资源规则、编辑器偏好、默认 UI backend；`ProjectSettingsStore` 直接代理 Hosting `ProjectSettings.json`，保存后同步 `EditorProject` / `project.pixelproj`，重开工程时回读并驱动 `ToEngineProject()` 的 content root、script dir 与 start scene。
- [x] **Player Settings**：已通过 plan/18 `PlayerSettingsDto` 绑定窗口标题、分辨率、VSync、图标、版本、启动场景、输入默认、运行时 UI backend、发行通道；`PlayerSettingsEditorAdapter` 保证 headless/runtime options、BuildRequest、build-player 参数、`build-result.json` 与 packaged `startup.json` 消费同源 DTO。
- [x] **Build Settings**：已通过 plan/18 `BuildProfileDto` 同源投影绑定 RID/channel/configuration、R2R/AOT、入包场景、启动场景、输出目录、符号、Build/Build And Run；`BuildSettingsStore` 读写 Hosting `BuildSettings.json`，消除 shell 内独立构建设置 schema 与 Hosting schema 的漂移风险。
- [x] **Settings JSON 韧性（自动化切片）**：Project / Player Settings JSON 中的 `resourceRules`、`editorPreferences`、`inputDefaults` 若被外部工具写成 `null`，Hosting DTO normalize 会回退为默认对象，避免 Settings 面板 / runtime projection 后续空引用；Build Settings 的 `scenes` 中若出现 `null` 条目，会返回“场景条目不能为空”的可执行诊断而不是空引用崩溃。证据：`EditorShellBuildTests.EngineProjectSettingsStoreCoalescesNullNestedSettingsAndReportsNullBuildScenes`。
- [x] **EditorShell fallback 材质查询（自动化切片）**：无内容包 / fallback 工程的 `ShellMaterialQuery` 已与 Hosting 材质门面保持 `MaterialInfo` 字段口径，发布 DisplayName、LegendCategory、LegendVisible、BaseColorBgra、MineYield、CellType、Category、Emissive、Hardness、MaxIntegrity、IsDestructible、FlowRate、BlocksCharacter、Flammability、AutoIgnitionTemp、FireHp、TemperatureOfFire、GeneratesSmoke、HeatConduct、HeatCapacity、RenderStyle 与 Properties，避免 Editor fallback 场景把可玩性 / 图例 / 破坏 / 燃烧 / 温度字段退化为仅 Name/Density/IsSolid。证据：`EditorShellSceneMaterializationTests.ShellFallbackMaterialQueryPublishesFullMaterialInfo`；真实材质内容质量与窗口 UX 仍按 M15 `[!]` 保留。
- [x] **Console 产品面（自动化切片）**：已在 EditorShell 层新增统一 `EditorConsoleStore` / `IEditorConsoleSink` / `EditorConsoleFilter` 模型，Console 面板支持 category / min severity / source / text / timestamp 排序筛选；Build Settings 保留局部 `_log`，同时将 build-player NDJSON/stdout/stderr、preflight、build result、warnings、Build And Run / path opener 失败写入统一 Console；脚本 watcher 热重载编译/应用诊断由 Hosting-owned `ScriptRuntime` 通过 `IScriptHotReloadDiagnosticSink` 写入 Console，EditorShell 不自行装配脚本 runtime；watcher 启动失败（含合法但不存在的 `ScriptSourceDir`）已降级为 `WatcherStartFailed` Console 诊断并继续运行无 watcher 的 scripting runtime，同时不吞掉真正致命的热重载装配错误；asset opener 结构化结果与 UI backend fallback 诊断均进入统一入口。证据：`EditorConsoleStoreTests`、`DemoRuntimeScriptingTests.AttachScriptingFromServicesReportsWatchedHotReloadCompileDiagnosticsToConsoleSink`、`DemoRuntimeScriptingTests.AttachScriptingFromServicesReportsMissingWatcherSourceAndKeepsRuntimeRunning`、`DemoRuntimeScriptingTests.AttachScriptingFromServicesDoesNotSwallowFatalHotReloadConfigurationErrors`、`EditorShellProjectTests.OpenAllowsMissingScriptSourceDirectoryAndReportsWatcherStartFailedToConsole`、`HostingProjectDisciplineTests.EditorShellConsumesHostingOwnedScriptingRuntimeAndConsoleDiagnostics`；真实窗口 Console 可读性与人工 UX 仍归 M15 `[!]`。
- [x] **Game View viewport/input 自动化契约底座**：已新增 Shell 层 `EditorGameViewContract` / `GameViewPanel` / `GameViewViewportSnapshot` / `GameViewUiInputSource` / `GameViewUiPresentTargetProvider`，默认 dock 与 Window 菜单将 Scene View、Game View 分成两个独立面板；Scene View 明确保持 authoring camera、gizmo/画刷/点选等工具输入且不冒充玩家视角，Scene View 与 Game View 均消费 `RenderPipeline.CurrentViewportTexture` 的当前自动化底座事实；Game View 记录 image rect、texture size、fit scale、visible viewport rect、panel-local → viewport texture 坐标映射与 framebuffer-space present target，Play 模式声明 Game UI → gameplay 输入让位策略、UI clip rect、Web-first UI hit-test 坐标来源与 editor overlay 优先级，并将 `InputFocused` / `InputHovered` 从整窗口收紧到实际图像区域。`IGameUiInputSourceFactory` 允许 EditorShell 将 Hosting 默认窗口输入包装为 Game View aware 输入源，把 panel-local 图像点裁剪/映射为 viewport texture pixels 后真实喂给 `UiInputRouter.HitTest` / `FeedPointerMove`，并只在 Play + Game View 聚焦 + 指针位于图像区域内时转发键盘、committed text 与 synthetic composition 输入，非目标区域会 drain 文本/composition 队列但不喂给 Web-first UI，保留 editor overlay 优先、透明区 pass-through 与交互区 capture 自动化语义；`IUiPresentTargetProvider` 允许 `UiLayerCompositor` 仅在 Play 模式把 direct Web-first UI present target 覆盖到 Game View image rect，Edit 模式不把游戏 UI 输出裁剪到 Game View，避免编辑态 UI 层被错误定向；`UiOffscreenSurfacePresenter` 默认使用 `UiPresentContext.Target` 提交离屏 surface，`ManagedFallbackBackend.Composite` 直接 Gui frame 也优先使用 `UiPresentContext.Target` 的宽高，`RmlUiBackend.Composite` 直连 native renderer 路径会通过 `peui_native_renderer_set_viewport_region` 按 `UiPresentContext.Target.X/Y/Width/Height` 更新 RmlUi viewport/context dimensions 与默认 framebuffer 输出 offset，避免输出侧只停在纯计算函数、只改宽高不改区域，或 fallback/RmlUi/未来离屏 UI 后端误用整窗口 viewport。`EditorShellGameViewContractTests` 覆盖 Scene/Game View 标题、相机 ownership、Play 输入策略、runtime viewport texture、overlay order、Game View 未聚焦阻断、Scene View 输入阻断、面板空白区阻断、图像内透明区 pass-through、交互 UI capture、mapped viewport 坐标进入 UI router、Game View 键盘/文本/composition 焦点门控、合法图像区域内透传与 Game View present target provider 的 Play/Edit 模式门控闭环。
- [ ] **Game View 产品剩余项**：Play 模式 Game View 中 Web-first UI 的真实窗口点击链路、透明合成视频/截图、手感与 M15 人工 UX 证据仍未闭合；当前已闭合 viewport geometry、input mapping、mapped coordinate → UI router、输出 target/clip 与 EditorShell Game UI runtime 装配的自动化 / GL smoke 闭环，继续归 §5 `[!]` 与后续真实窗口验收。
- [x] **默认工作台体验（自动化路线底座）**：已新增 `--scripted-default-workbench-probe`，在无 `--project` 时自动新建临时工程，显示默认核心面板，创建 GameObject，生成默认脚本源文件，经 Hosting-owned hot reload 将动态脚本程序集注册进 `ScriptAssemblyRegistry`，确认 `DefaultWorkbenchBehaviour` 可被 Behaviour 列表发现并直接挂载到当前 GameObject；authoring projection 刷新可在脚本 runtime 已接入后替换当前脚本 Scene，并同步 Hosting current scene、service locator、script context 与 hot reload target scene；真实 ImGui 后端已为 ImGuizmo 绑定同一 ImGui context，避免选中对象后 Scene View gizmo native 崩溃；随后保存场景，进入/退出 Play，打开 Build Settings，并复用同一 `TryStartScriptedBuildProbe` / build-player 子进程路线实际等待 `build-result.json` 回流，输出 `editor_default_workbench_probe` 结构化摘要（含 `script_hot_reload_applied` / `behaviour_registered` / `behaviour_attached` / `build_started` / `build_completed` / `build_ok` / `build_package_archive`）；2026-07-08 真实窗口短跑 `--scripted-default-workbench-probe --window-ticks 80 --capture-frame artifacts/editor-dpi-frame-after-hexa-framebuffer-fix.bmp` 验证 Hexa OpenGL 后端高 DPI 下默认工作台截图为 1280x720、非黑内容 bbox=0,0-1279,719、四象限非黑像素均为 230400，不再只绘制左下 1/4；2026-07-08 默认工作台玩家包修复 `tools/package.ps1` 入包工程 `scripts/`，Demo runtime 在 scene 物化前编译注册 `content/scripts`，并在缺少完整材质内容包的空工程 Probe 中接入最小可渲染 world，`Default Workbench Probe.exe --window-ticks 5 --capture-frame artifacts/editor-default-workbench-fixed/probe-run.bmp` 已完成 5 帧窗口短跑且不再因 `DefaultWorkbenchBehaviour` 或 `SimulationPhaseDriver` 缺失闪退；该切片只闭合 headless/scripted 连续路线、玩家包构建自动化底座、产物短跑 smoke 与 DPI 布局 smoke，不冒充真实窗口人工 UX 或 Build And Run 点击手感。
- [ ] **默认工作台产品剩余项**：真实窗口首次启动手感、Build And Run 点击链路、截图/视频、产物人工运行与人工 UX 复核仍未闭合。

## 5. M15 人工 UX 证据 checklist

- [!] 真实窗口完整路线：需要同一 `reviewSessionId` / `gitCommit` 的视频或等价人工复核材料，覆盖启动 Shell、新建/打开工程、默认布局、Project/Hierarchy/Inspector/Scene View/Game View/Console、Play/Exit、保存、Build And Run。
- [!] Project Window 引用稳定性：需要人工或端到端证据证明资产移动/重命名/删除确认后 Prefab、Scene、Inspector 字段与 Build 入包不丢引用；当前 Project Window move request、删除安全预检、引用阻塞、Scene 移动同步 Project/Player/Build settings 与 BuildRequest 的单元测试只能作为辅助，不能替代真实窗口/端到端证据。后续证据应至少绑定当前 HEAD，覆盖 prefab、scene、texture/script/material/audio 的 stable id 前后、活动 scene、磁盘 `.scene/.prefab`、Project/Player/Build/startup、BuildRequest 和一次打包结果。
- [!] 脚本外部编辑器：需要真实 OS opener / configured editor 的打开证据、失败提示截图或日志；不能用“按钮存在”作为完成。
- [!] Settings UX：Project/Player/Build Settings 仍需要真实窗口填写、保存、重启恢复、错误输入校验和 build-player 参数投影的人工/截图/视频证据；本轮 Project/Player/Build Settings 的 Hosting DTO 同源读写、错误输入不保存、scripted probe 与 headless/build-player 投影已有自动化覆盖，但不能替代最终 UX 复核。
- [!] Editor 产品可用性：需要人工确认布局、快捷键、拖拽、gizmo、Undo/Redo、Console 错误、Build 面板反馈在目标窗口环境中可理解、可恢复、无阻塞 UX 缺陷。
- [x] Editor UX evidence preflight 入口：`tools/editor-ux-evidence-preflight.ps1` 已登记为 M15 人工 UX 证据入口，缺 manifest 输出 `blocked_missing_editor_ux_evidence`，schema / HEAD / report 元数据无效输出 `blocked_invalid_editor_ux_evidence`，缺 scope、hash 不匹配、scope 语义字段不足或数值下限不足输出 `blocked_missing_editor_ux_scope_evidence`，证据齐全仍输出非零 `editor_ux_evidence_attached_pending_review` 等待人工复核；必需 scope 为 `editor_full_route_window`、`project_window_reference_stability`、`script_external_editor`、`settings_build_ux`、`editor_product_usability`，并要求真实窗口路线报告证明 `editorShellExeLaunched`、`singleTopLevelWindowVerified`、`singleProcessInProcessHost`、`noConsoleWindowObserved`、视频时长/帧数，Project Window 前后 stable id、Scene/Prefab/Inspector/Project/Player/Build/startup/BuildRequest/Build 包引用审计、零断引用、资产类型覆盖、脚本打开次数、Settings 往返/Build And Run 次数和产品可用性 checklist/reviewer 数等机器可读下限。该工具只收紧证据入口，不替代真实窗口视频/截图、外部编辑器、拖拽/gizmo/Undo/Redo 与人工 UX 评审。
- [!] 证据状态边界：`scripted_probe_only`、截图、短跑 smoke、`manual_evidence_attached_pending_review` 只能说明证据入口可用，不能把 M15 UX 验收转为 [x]；人工 evidence manifest 必须绑定当前 HEAD，report 正文必须写明同一 `reviewSessionId` / `gitCommit`、结论与风险，旧提交或弱说明会被预检拒绝。

## 6. 验证命令与证据路径 checklist

- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --filter "FullyQualifiedName~EditorAssetDropServiceTests|FullyQualifiedName~EditorProjectAssetModelTests|FullyQualifiedName~EditorScriptAssetOpenServiceTests"` 覆盖 Project Window stable asset id/manifest/logical path/type、常见资产创建、Project Window 数据源消费与只读预览摘要、prefab move/rename 引用保持、Hierarchy / Scene View / Inspector typed asset field / script component list 资产拖拽语义、Project Window browser payload → GameObject Inspector drop target 接线、drop 成功/失败诊断进入 Inspector 状态与 Console、无效 drop 诊断、资产移动后 Inspector asset reference 重写，以及脚本外部编辑器 configured `{file}` 替换 / 无 `{file}` 自动追加脚本路径 / fallback / 失败 / 拒绝 / 路径安全自动化切片；2026-07-07 本地运行 `dotnet test tests\PixelEngine.Hosting.Tests\PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~EditorAssetDropServiceTests|FullyQualifiedName~EditorShellSceneMaterializationTests|FullyQualifiedName~EditorShellProjectTests|FullyQualifiedName~GameObjectInspector"` 通过 21/21，新增覆盖 private `[SerializeField][AssetField] ScriptAssetReference` 在 runtime projection 与 `.scene` loader 的统一绑定，以及 `[HideInInspector]` / readonly 拒绝。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~EditorProjectAssetModelTests"` 覆盖 Project Window stable asset id、manifest、logical path、prefab move/rename 引用保持、Project Window move request 回查 stable id 后重写 typed asset reference、Folder recursive move 保留子资产 stable id 并重写 typed asset reference、UI Screen 文件夹移动同步 `ui-manifest.json` path 且拒绝改变资产类型的目录移动、UI Manifest 面板同步现有未登记 UI Screen 并切换 preload、Folder recursive delete 绑定子资产 stable id 集合并在仍被外部 Scene / 活动 authoring 模型引用时拒绝、未引用目录二次确认后递归删除磁盘 / manifest / UI Screen manifest entry、移动前解析全部引用文档并在损坏 scene/prefab 时阻断半提交、引用文档保存到一半失败后恢复已写文档且活动 authoring 场景仍保留旧引用、Scene 资产移动同步 `project.pixelproj` / Project Settings / Player Settings / Build Settings / `startup.json` 与 BuildRequest、删除预检 / 二次确认，以及纹理、音频、材质 JSON、场景、Prefab、脚本的只读预览摘要，2026-07-10 本地定向验证通过；`dotnet test tests/PixelEngine.Editor.Tests/PixelEngine.Editor.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~AssetBrowserPanelTests"` 覆盖 Project Window 面板搜索、类型过滤、路径/类型/最近修改/大小排序、选择、音频试听、typed drag payload、脚本外部打开、stable id 删除请求、二次确认回调、旧数据源项拒绝删除、move/rename 请求必须携带 stable id、成功后刷新资产列表、folder target drag/drop request、Folder move callback 成功后刷新资产和文件夹目标、Folder delete callback 携带子资产 stable id 集合并在确认后刷新资产和文件夹目标、Create 输入跟随选中文件夹并自动避开已有 logical path、旧 drag payload 拒绝，以及同路径资产刷新为新 id 后旧删除 / 移动确认失效，2026-07-10 本地通过。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --filter "FullyQualifiedName~DemoRuntimeScriptingTests|FullyQualifiedName~EditorShellProjectTests|FullyQualifiedName~EditorConsoleStoreTests|FullyQualifiedName~HostingProjectDisciplineTests"` 覆盖 Console store/filter/capacity/sort、build-player/preflight/result sink、asset opener / UI backend fallback / script diagnostics 归类、Hosting-owned runtime 诊断 sink 装配边界、watcher 热重载编译失败不静默、合法但不存在的 `ScriptSourceDir` 降级为 `WatcherStartFailed` 且不阻塞 `AttachScriptingFromServices`、真正致命热重载装配错误仍抛出；本轮同一过滤器新增覆盖 `--scripted-default-workbench-probe` 参数解析与默认工作台 scripted route 源码契约（新建工程、核心面板、GameObject、脚本源、保存、Play/Exit、Build Settings 入口），2026-07-07 本地通过 45/45；2026-07-08 本地运行 `dotnet test tests\PixelEngine.Hosting.Tests\PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~DemoRuntimeScriptingTests|FullyQualifiedName~HostingProjectDisciplineTests|FullyQualifiedName~SceneAndHeadlessTests"` 通过 62/62，新增覆盖 Hosting-owned hot reload 将新脚本动态程序集注册进 `ScriptAssemblyRegistry`、默认工作台脚本可被 script drop / Add Component 路径发现并挂载、authoring projection 刷新后脚本 Scene replacement 同步到 Hosting/service/script context/hot reload target、ImGuizmo 绑定 ImGui context 的真实窗口 gizmo 回归纪律，以及 `editor_default_workbench_probe` 输出 `script_hot_reload_applied` / `behaviour_registered` / `behaviour_attached`。2026-07-08 本轮同一源码纪律测试追加锁定默认工作台 probe 复用 `TryStartScriptedBuildProbe` / `CaptureScriptedBuildProbe` 并输出 `build_started` / `build_completed` / `build_ok` / `build_package_archive`，本地定向验证通过。其中 `EditorProjectSession.Open` 真实窗口依赖路径需配合单测环境变量单独复验。
- [x] `$env:PIXELENGINE_RENDERING_GL_SMOKE = '1'; dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --filter "FullyQualifiedName~OpenAllowsMissingScriptSourceDirectoryAndReportsWatcherStartFailedToConsole"` 覆盖合法但不存在的 `ScriptSourceDir` 降级为 `WatcherStartFailed` 且不阻塞 `EditorProjectSession.Open`，当前通过 1/1。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~EditorShellSceneMaterializationTests"` 覆盖 EditorShell fallback `MaterialInfo` 完整字段口径、运行时 authoring projection、Prefab、嵌套 Prefab、stable asset reference loader 与隐藏 / readonly 字段拒绝，2026-07-08 本地通过 8/8。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --filter "FullyQualifiedName~EditorProjectAssetModelTests|FullyQualifiedName~EditorShellSceneMaterializationTests"` 覆盖 Project Window stable asset id/manifest/logical path/type、常见资产创建、Project Window 数据源消费、prefab move/rename 引用保持、Project Window move request 引用重写、移动前引用文档预检、引用文档保存失败回滚与既有 prefab 场景物化回归；当前定向拆分验证为 `EditorProjectAssetModelTests` 10/10 与 `EditorShellSceneMaterializationTests` 8/8。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --filter "FullyQualifiedName~EditorShellGameViewContractTests|FullyQualifiedName~InputArbitratorTests|FullyQualifiedName~EngineWindowOwnershipTests"` 覆盖 Scene View / Game View 独立产品契约、Play 模式 Game UI → gameplay 输入让位、Game View 未聚焦阻断、Scene View authoring 输入阻断、editor overlay 捕获优先、Game View image rect / fit scale / panel-local→viewport 映射、面板空白区阻断、图像内透明区 pass-through、交互 UI capture、`GameViewUiInputSource` mapped viewport 坐标进入 `UiInputRouter` hit-test / pointer feed、Game View 键盘/文本/composition 焦点门控、合法图像区域内透传、Game View framebuffer-space present target provider、Hosting direct UI compositor 消费 provider 与输入仲裁回归，2026-07-08 本地通过；`dotnet test tests/PixelEngine.UI.Tests/PixelEngine.UI.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~UiOffscreenSurfacePresenterSmokeTests"` 覆盖离屏 UI surface 默认使用 `UiPresentContext.Target` 输出，防止 Game View 内嵌目标被后端显式 viewport 误覆盖；本轮 `dotnet test tests\PixelEngine.UI.Tests\PixelEngine.UI.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~ManagedFallbackBackendTests"` 通过 10/10，补齐 ManagedFallback 直接 composite 路径对 `UiPresentContext.Target` 宽高的源码契约和 default context 退回初始化 viewport 尺寸；本轮 `dotnet test tests\PixelEngine.UI.Tests\PixelEngine.UI.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~RmlUiGlBootstrapSmokeTests|FullyQualifiedName~ManagedFallbackBackendTests|FullyQualifiedName~UiOffscreenSurfacePresenterSmokeTests"` 通过 18/18，补齐 RmlUi direct native composite 路径对 `UiPresentContext.Target.Width/Height` 的 viewport/context dimensions 契约；本轮 `tools\build-native.ps1 -Rid win-x64 -Configuration Release` 通过，随后 `dotnet test tests\PixelEngine.UI.Tests\PixelEngine.UI.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~RmlUiGlBootstrapSmokeTests|FullyQualifiedName~UiContractTests|FullyQualifiedName~ManagedFallbackBackendTests|FullyQualifiedName~UiOffscreenSurfacePresenterSmokeTests"` 通过 34/34，`$env:PIXELENGINE_RENDERING_GL_SMOKE='1'; dotnet test tests\PixelEngine.UI.Tests\PixelEngine.UI.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~RmlUiGlBootstrapSmokeTests"` 通过 6/6，补齐 RmlUi direct native composite 路径对 `UiPresentContext.Target.X/Y/Width/Height` 的完整 framebuffer 输出区域契约；`dotnet test tests/PixelEngine.Editor.Tests/PixelEngine.Editor.Tests.csproj -c Release --filter "FullyQualifiedName~EditorAppTests"` 覆盖默认 dock/window title 回归，2026-07-07 本地通过 10/10。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~RecentProjects"` 覆盖最近工程 `recent-projects.json` 损坏容错、空/非法 ProjectPath 跳过、重复路径保留最新、倒序排序与 20 条裁剪，2026-07-08 本地通过 2/2。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~HostingProjectDisciplineTests"` 覆盖 Demo / EditorShell 依赖纪律、WinExe 子系统、ProjectPicker 菜单布局、Windows 原生文件夹选择器初始目录接线、默认工作台 / BuildSettings / Console / Game View / Demo 公开 API 纪律；2026-07-09 本地通过 35/35。本轮新增定向契约 `UserFacingLaunchersCreateNoConsoleWindows`，锁定 Editor Build And Run 与 `tools/update-final-output.ps1` 的子进程启动均显式 `CreateNoWindow=true`，避免正式应用或验证链路弹出控制台窗口。
- [x] `dotnet test tests\PixelEngine.Hosting.Tests\PixelEngine.Hosting.Tests.csproj -c Release --no-restore --disable-build-servers -m:1 --filter "FullyQualifiedName~EditorShellProjectTests|FullyQualifiedName~EditorShellProjectPickerUsesNativeFolderDialogWithInitialPath"` 覆盖 ProjectPicker Browse 成功、失败、取消的路径 / 诊断契约，以及 Windows 原生文件夹选择器默认目录源码纪律；2026-07-09 本地定向验证通过 11/11。
- [x] `dotnet test tests/PixelEngine.Editor.Tests/PixelEngine.Editor.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~EditorInputBridgePublishesModifierKeysForClipboardShortcuts"` 与 `dotnet test tests/PixelEngine.UI.Tests/PixelEngine.UI.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~GuiInputBridgePublishesModifierKeysForClipboardShortcuts"` 分别覆盖 Editor 与中性 Gui 输入桥的 Ctrl+V modifier 聚合契约，2026-07-09 本地均通过 1/1。
- [x] `dotnet test tests/PixelEngine.Hosting.Tests/PixelEngine.Hosting.Tests.csproj -c Release --filter "FullyQualifiedName~EditorShellBuildTests|FullyQualifiedName~EngineBuilderTests"` 覆盖 Project/Player/Build Settings 同源 DTO/store、ProjectSettings → EditorProject/EngineProject、Project/Player Settings 面板 scripted probe、错误输入不保存、PlayerSettings → BuildRequest/runtime options/build-player 参数/`build-result.json` 投影与 EngineBuilder 窗口标题/启动场景；2026-07-08 本轮新增 Settings JSON nullable nested object / null scene 条目韧性覆盖，本地运行 `dotnet test tests\PixelEngine.Hosting.Tests\PixelEngine.Hosting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~EditorShellBuildTests|FullyQualifiedName~EngineBuilderTests"` 通过 32/32。
- [x] `dotnet test tests/PixelEngine.Editor.Shell.Tests/PixelEngine.Editor.Shell.Tests.csproj -c Release --filter FullyQualifiedName~EditorShellProjectTests|FullyQualifiedName~EditorScene|FullyQualifiedName~Prefab|FullyQualifiedName~PlayerBuildService` 覆盖工程模型、场景往返、Prefab 与 build-player 编排。
- [x] `dotnet test tests/PixelEngine.Demo.Tests/PixelEngine.Demo.Tests.csproj -c Release --filter FullyQualifiedName~PlayerOnly|FullyQualifiedName~DemoStartupOptionsTests` 覆盖玩家包解耦与 startup 分派边界。
- [x] 2026-07-08 `dotnet test tests\PixelEngine.Demo.Tests\PixelEngine.Demo.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~DemoStartupOptionsTests"` 通过 24/24，覆盖玩家包 `content/scripts` 在 scene 物化前注册与空内容 Probe 最小可渲染 world；`dotnet test tests\PixelEngine.Scripting.Tests\PixelEngine.Scripting.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~RuntimeScriptAssemblyCompilerTests|FullyQualifiedName~ScriptCompilerTests"` 通过 4/4，覆盖运行时脚本目录编译与 no-PDB 编译路径；`tools\build-player.ps1 -Rid win-x64 -Channel r2r ... -Output artifacts\editor-default-workbench-fixed` build/audit 通过，`artifacts\editor-default-workbench-fixed\player\Default Workbench Probe.exe --window-ticks 5 --capture-frame artifacts\editor-default-workbench-fixed\probe-run.bmp` 退出码 0。
- [x] `tools/build-player.ps1` / `tools/build-player.sh` 与 `tools/audit-release-artifacts.*` 是 Build Settings 面板消费的出包与 player-only 审计真相源。
- [x] `docs/runtime-reports/2026-07-06-editor-shell-attach-probe.md` 是当前 Shell attach / scripted probe 证据路径。
- [x] `tools/editor-ux-evidence-preflight.ps1` 是 plan/19 M15 UX 证据预检入口；manifest `gitCommit` 必须等于当前 HEAD，report 类证据必须包含同一 `reviewSessionId` / `gitCommit`、结论、风险、必需 true 字段与必需数值下限，并覆盖 `editor_full_route_window`、`project_window_reference_stability`、`script_external_editor`、`settings_build_ux`、`editor_product_usability` 五个 scope；完整路线 scope 必须证明正式 Shell EXE、单顶层窗口、单进程 in-process host 和无控制台窗口；`editor_ux_evidence_attached_pending_review` 仍是待人工复核状态，不代表 M15 完成。
- [!] `tools/demo-manual-acceptance-preflight.ps1` 只提供人工验收证据入口；缺合格 manifest、真实窗口材料与人工复核前，M15 UX 证据保持阻塞。

## 7. 依赖与下一闭合节点 checklist

- [x] 上游依赖：plan/00 依赖方向与技术栈、plan/12 Editor ImGui 面板层、plan/18 Hosting attach/Edit/Play/scene writer、plan/11 脚本与 Scene 模型、plan/08 RenderWindow/GL/UI 层、plan/15 build-player 与 player-only audit 已登记为 Shell 的公开 API 边界。
- [x] 下游消费：plan/13 Demo 使用 player-only 解耦后的公开 runtime；plan/20 复用 `PixelEngine.Gui` 字体与 ManagedFallback；plan/14 负责 shell scripted probe、scene/prefab/build tests；plan/17 只登记 M13/M14/M15 DAG 与退出标准。
- [x] 本轮闭合节点：plan/18 `ProjectSettingsDto` / `PlayerSettingsDto` 已绑定到 Project Settings / Player Settings 面板、EditorProject/EngineProject 入口与 headless/runtime/build-player/package 消费路径；真实 Settings UX 保存、重启恢复、人工填写和截图证据仍归 M15 `[!]`。
- [x] 本轮闭合节点：Project Window stable asset id/manifest、logical path、asset type、Project Window 数据源消费、搜索 / 类型过滤 / 排序、只读预览摘要、prefab 移动/重命名引用保持、按钮 + 新路径输入的 move/rename 请求入口、移动前引用文档预检、删除确认与引用安全预检自动化切片已落地；删除与移动确认均绑定原始 stable asset id / logical path / asset type，同路径资产刷新为新 id 后旧确认失效，损坏 `.scene/.prefab` 会在资产移动前阻断；真实窗口拖拽移动、真实窗口删除确认 UX、脚本外部编辑器人工打开、真实窗口预览布局与人工 UX 证据仍保留为后续节点。
- [x] 本轮闭合节点：Scene 资产移动同步自动化切片已落地，Project Window Shell 数据源通过 `EditorProjectSceneAssetMoveService` 协调 manifest 文件移动、`project.pixelproj`、Project Settings、Player Settings、Build Settings 与 `content/startup.json`，并验证 BuildRequest 的 StartScene / IncludedScenes 使用新路径；真实窗口拖拽移动、重命名手感和人工 UX 证据仍保留为后续节点。
- [x] 本轮闭合节点：资产拖拽语义自动化切片已落地，typed payload 携带 stable asset id / logical path / asset type，drop result 强制携带诊断；Project Window 行项已注册 ImGui drag source，Inspector typed asset field 已注册 drop target 并消费 browser payload，成功/失败诊断写回 Inspector 状态与 Console；prefab→Hierarchy/Scene View、scene/material/script/texture/audio→Inspector typed field、script→组件列表与无效 drop 无副作用均由 `EditorAssetDropServiceTests` 覆盖，Project Window 侧 payload 创建由 `AssetBrowserPanelTests` 覆盖；真实窗口拖拽手感与人工 UX 仍归 M15 `[!]`。
- [x] 本轮闭合节点：脚本外部编辑器自动化切片已落地，Project Window script 资产双击调用 Shell opener，configured `ExternalScriptEditor` 优先、未配置回退系统默认 opener，失败/拒绝/路径安全诊断可见并由 `AssetBrowserPanelTests` / `EditorScriptAssetOpenServiceTests` 覆盖；真实 OS opener / configured editor 打开材料仍归 M15 `[!]`。
- [x] 本轮闭合节点：Console 产品面自动化切片已落地，统一 Console store/sink/filter model 汇总 build-player NDJSON/stderr/result、Hosting-owned 脚本热重载诊断、UI backend fallback、asset opener 与 project/build path 错误；`EditorConsolePanel` 提供 category / min severity / source / text 筛选、newest-first 排序与清空入口；真实窗口 Console 可读性、布局与人工 UX 仍归 M15 `[!]`。
- [x] 本轮闭合节点：Game View viewport/input 自动化契约底座已落地，`EditorGameViewContract` 明确 Scene View / Game View 的相机 ownership、Play 输入策略、runtime viewport texture、UI clip rect、viewport coordinate space、Web-first UI hit-test 坐标来源和 game/editor overlay 顺序，`GameViewPanel` 记录 `GameViewViewportSnapshot` 并将输入焦点/hover 收紧到实际图像区域；本轮追加 `GameViewUiInputSource` 键盘、committed text 与 composition 焦点门控，只在 Play + 聚焦 + 图像区域内透传给 Web-first UI，非目标区域 drain 但不注入；本轮补齐 Game View 输出侧自动化契约，`GameViewViewportSnapshot.TryCreateUiPresentTarget` 可把 panel-local image rect 转成 framebuffer-space `UiPresentTarget`，`EditorViewportContract` 区分输入 clip 与输出 clip，`GameViewUiPresentTargetProvider` 将该 target 接入 `UiLayerCompositor` direct 后端路径，测试锁定 Game View 的 Web-first UI 输出裁剪目标来自 image rect；本轮进一步把 `UiOffscreenSurfacePresenter` 默认输出区域、`ManagedFallbackBackend.Composite` 直接 Gui frame 尺寸和 `RmlUiBackend.Composite` direct native viewport/context dimensions 收敛到 `UiPresentContext.Target`，让 fallback、RmlUi 与未来离屏 UI 后端复用同一裁剪目标宽高而不是自带整窗口 viewport；本轮再补 `peui_native_renderer_set_viewport_region` 与 `RmlUiNative.RendererSetViewportRegion`，使 RmlUi direct native 路径同步使用 target 的 X/Y/Width/Height 完整 framebuffer 输出区域，而不是只同步宽高；2026-07-08 追加高 DPI 修复：Editor/Gui Hexa ImGui backend 以逻辑尺寸输入、按 framebuffer scale 换算为 Hexa OpenGL 后端实际消费的 framebuffer `DisplaySize`，并同步缩放鼠标坐标，`UiPresentContext` 暴露 logical size / framebuffer scale，EditorShell picker、GuiRenderBridge、EditorRenderBridge 与 Game View UI present target 均消费同一缩放；短跑真实 shell 截图 `artifacts/editor-dpi-frame-after-hexa-framebuffer-fix.bmp` 验证 1280×720 framebuffer 非黑 bbox 覆盖 `(0,0)-(1279,719)`，四象限非黑像素均为 230400，不再只渲染左下 1/4；2026-07-09 追加点击坐标可靠性修复：中性 `GuiWindowInputConnector` 与 EditorShell `EditorWindowInputConnector` 在 MouseDown/MouseUp 前读取当前 `mouse.Position`，按事件时 `RenderWindow.FramebufferScaleX/Y` 换算并通过 `MouseMoveFramebuffer` 直接注入 framebuffer 坐标，避免窗口切换、高 DPI、resize/DPI 切换后一帧旧 `_frameMetrics` 或缺少最新 MouseMove 时按钮命中使用旧坐标；2026-07-08 追加 EditorShell Game UI runtime 装配：`PlayerSettingsEditorAdapter.ApplyRuntimeDefaults` 启用 GUI/Game UI，`EditorProjectSession.Open` 不再关闭 runtime 并在 builder 链上显式 `.UseGuiRuntime().EnableGameUi()`，GL smoke 证明打开工程后注册 `GameUiHost` / `IGameUiService` / `GameUiBackendSelection` 并产生 ManagedFallback fallback console 诊断；Play 模式 Game View 的真实窗口点击链路、完整人工走查与 UX 材料仍归 M15 `[!]`。
- [x] 本轮闭合节点：默认工作台 scripted route 底座已落地，`--scripted-default-workbench-probe` 串联无工程首次启动自动新建工程、默认核心面板显示、GameObject 创建、默认脚本源生成、Hosting-owned hot reload 注册动态脚本程序集、`DefaultWorkbenchBehaviour` 被 Behaviour 列表发现并直接挂载、场景保存、Play/Exit、Build Settings 入口，并复用 build-player 子进程路线等待玩家包构建结果回流，输出 `editor_default_workbench_probe` 的 `build_started` / `build_completed` / `build_ok` / `build_package_archive`；真实窗口首次启动手感、Build And Run 点击链路、产物人工运行与人工 UX 证据仍归后续节点。
- [!] M15 后续节点：补真实窗口人工 UX 材料，完成后再同步 README/plan17 dashboard；不得用 scripted probe 替代人工 UX 完成态。
